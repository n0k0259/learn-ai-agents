---
title: Style guide
description: Reference for the components and diagram conventions used in the lessons.
pagefind: false
---
import Quiz from '~/components/Quiz.astro';
import KeyTakeaways from '~/components/KeyTakeaways.astro';
import Figure from '~/components/Figure.astro';
import StepAnimator from '~/components/StepAnimator.astro';
import ConceptLegend from '~/components/diagrams/ConceptLegend.astro';
import AgentFormula from '~/components/diagrams/chapter-1/AgentFormula.astro';
import AgentEnvironmentLoop from '~/components/diagrams/chapter-1/AgentEnvironmentLoop.astro';
import ObservationActionSpaces from '~/components/diagrams/chapter-1/ObservationActionSpaces.astro';
import CapabilityUpdateLevels from '~/components/diagrams/chapter-1/CapabilityUpdateLevels.astro';
import ContextAblation from '~/components/diagrams/chapter-1/ContextAblation.astro';
import ReActLoop from '~/components/diagrams/chapter-1/ReActLoop.astro';
import TrajectoryRounds from '~/components/diagrams/chapter-1/TrajectoryRounds.astro';
import ModelAsAgent from '~/components/diagrams/chapter-1/ModelAsAgent.astro';

This page documents the building blocks every lesson uses. It is not part of the tutorial itself.

## Lesson components

:::tip[TL;DR]
Every lesson opens with a two-to-three line summary like this one.
:::

:::note[Engineer's note]
Practical implications and pitfalls go in callouts like this.
:::

<KeyTakeaways>
- Takeaways are a short bullet list.
- One idea per bullet.
</KeyTakeaways>

<Quiz question="What does a quiz look like?">
A question with a click-to-reveal answer.
</Quiz>

## Diagram conventions

Every diagram uses the same color for the same concept. Press **Step** to walk through the legend.

<StepAnimator label="Concept legend">
<Figure caption="The concept color legend used by every diagram.">
<ConceptLegend />
</Figure>
</StepAnimator>

- Colors come only from `src/styles/diagrams.css`; each color is always paired with a text label.
- Boxes are `g.dg-node` elements (one `rect` + direct `text` children); groups are `g.dg-container`.
- Wide diagrams scroll sideways on phones rather than shrinking text below 14px.

## All diagrams

Each lesson adds its diagrams below, grouped by lesson.

### Lesson 1.1

<Figure caption="Agent = LLM + Context + Tools. The environment sits outside the agent boundary.">
<AgentFormula />
</Figure>

<StepAnimator label="Agent–environment loop">
<Figure caption="The agent–environment loop and the Model–Harness structure inside the agent." credit="Redrawn from Figure 1-1, AI Agent Book by Bojie Li (Apache 2.0).">
<AgentEnvironmentLoop />
</Figure>
</StepAnimator>

### Lesson 1.2

<Figure caption="The observation space carries the world into the model's context; the action space carries the model's decisions back out as operations.">
<ObservationActionSpaces />
</Figure>

### Lesson 1.3

<Figure caption="Three levels of agent capability updates, from fast and temporary context changes to slow and permanent parameter updates." credit="Redrawn from Figure 1-2, AI Agent Book by Bojie Li (Apache 2.0).">
<CapabilityUpdateLevels />
</Figure>

### Lesson 1.4

<Figure caption="Experiment 1-1: five controlled groups. The full baseline keeps every component; each other group removes one (✗) and shows the outcome." credit="Redrawn from Figure 1-3, AI Agent Book by Bojie Li (Apache 2.0).">
<ContextAblation />
</Figure>

### Lesson 1.5

<StepAnimator label="ReAct loop">
<Figure caption="The ReAct loop: reason, act, observe, and reason again. A response with no tool call ends the loop with the final answer.">
<ReActLoop />
</Figure>
</StepAnimator>

<StepAnimator label="Trajectory, round by round">
<Figure caption="The multi-currency trajectory, round by round. Every LLM call sees the static prefix plus the full trajectory so far." credit="Redrawn from Figure 1-4, AI Agent Book by Bojie Li (Apache 2.0).">
<TrajectoryRounds />
</Figure>
</StepAnimator>

<Figure caption="Model as Agent: the model decides the tool calls and native tools run server-side. The loop runs on the server only on the Responses API path." credit="Redrawn from Figure 1-5, AI Agent Book by Bojie Li (Apache 2.0).">
<ModelAsAgent />
</Figure>
